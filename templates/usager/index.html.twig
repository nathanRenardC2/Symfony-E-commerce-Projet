{% extends 'base.html.twig' %}

{% block title %}Shop projet | Mon compte{% endblock %}

{% block body %}
    {% if userAuthentificate %}
    <div class="flex items-center h-screen w-full justify-center">

        <div class="max-w-xs">
            <div class="bg-white shadow-xl rounded-lg py-3">
                <div class="p-2">
                    <h3 class="text-center text-xl text-gray-900 font-medium leading-8">{{userAuthentificate.prenom}} {{userAuthentificate.nom}}</h3>
                    <div class="text-center text-gray-400 text-xs font-semibold">
                        <p>
                            {% if is_granted('ROLE_ADMIN') %}
                                Admin 
                            {% else %}
                                Client
                            {% endif %}
                        </p>
                    </div>
                    <table class="text-xs my-3">
                        <tbody>
                        <tr>
                            <td class="px-2 py-2 text-gray-500 font-semibold">Email</td>
                            <td class="px-2 py-2">{{userAuthentificate.email}}</td>
                        </tr>
                    </tbody></table>
                </div>
            </div>
        </div>

    </div>
    {% else %}
        <div class="h-screen w-screen flex justify-center items-center">
            <p>Vous n'êtes pas connecté</p>
        </div>
    {% endif %}
{% endblock %}
