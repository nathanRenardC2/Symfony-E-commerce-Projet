{% extends 'base.html.twig' %}

{% block title %}Shop projet | Commande{% endblock %}

{% block body %}

<div class="w-screen flex flex-col items-center justify-center">
        <div class="overflow-x-auto h-screen w-full">
            <table class="table w-full">
                <!-- head -->
                <thead>
                <tr>
                    <th>N°</th>
                    <th>Date</th>
                    <th>Produits</th>
                    <th>Montant</th>
                    <th>Status</th>
                </tr>
                </thead>
                <tbody>
                <!-- row 1 -->
                {% for row in commandes %}
                    <tr>
                        {% if row.commande.statut is same as("En attente de validation") %}
                            <td class="text-red-500">
                                {{row.commande.id}}
                            </td>
                        {% else %}
                            <td>
                                {{row.commande.id}}
                            </td>
                        {% endif %}
                        {% if row.commande.statut is same as("En attente de validation") %}
                            <td class="text-red-500">
                                {{row.commande.dateCommande|date('d/m/Y H:i:s')}}
                            </td>
                        {% else %}
                            <td>
                                {{row.commande.dateCommande|date('d/m/Y H:i:s')}}
                            </td>
                        {% endif %}
                        {% if row.commande.statut is same as("En attente de validation") %}
                            <td class="text-red-500">
                                {{row.nbProduits}}
                            </td>
                        {% else %}
                            <td>
                                {{row.nbProduits}}
                            </td>
                        {% endif %}
                        {% if row.commande.statut is same as("En attente de validation") %}
                            <td class="text-red-500">
                                {{row.montant}} €
                            </td>
                        {% else %}
                            <td>
                                {{row.montant}} €
                            </td>
                        {% endif %}
                        {% if row.commande.statut is same as("En attente de validation") %}
                        <td>
                            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6 hover:cursor-progress text-red-500">
                                <path stroke-linecap="round" stroke-linejoin="round" d="M12 6v6h4.5m4.5 0a9 9 0 11-18 0 9 9 0 0118 0z" />
                            </svg>
                        </td>
                        {% endif %}
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
</div>


{% endblock %}
